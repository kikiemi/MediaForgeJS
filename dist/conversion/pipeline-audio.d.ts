import type { ConversionOptions } from './context.js';
/** WebCodecs audio plus the built-in AAC fallback for container transcoding. */
export declare const pipelineAudio: NonNullable<ConversionOptions['pipelineAudio']>;
