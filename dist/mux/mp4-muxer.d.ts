import type { Sink } from '../types/io.js';
import type { MuxerConfig, OutputMuxer } from '../types/container.js';
import type { EncodedChunk } from '../types/media.js';
export declare class MP4Muxer implements OutputMuxer {
    /** co64 + 64-bit mdat once the file passes the 32-bit box limits. */
    private use64BitOffsets;
    private readonly sink;
    private readonly cfg;
    private readonly videoChunks;
    private readonly audioChunks;
    private videoConfig?;
    private audioConfig?;
    private readonly extraVideoChunks;
    private readonly extraVideoConfigs;
    private activeVideoCfg;
    private activeVideoConfig;
    private readonly extraAudioChunks;
    private readonly extraAudioConfigs;
    private activeAudioCfg;
    private activeAudioConfig;
    private readonly fragmented;
    private initWritten;
    private moofSeq;
    private fragStartSec;
    private pendingV;
    private pendingA;
    private vFirstDtsSec;
    private aFirstDtsSec;
    private vLastDtsSec;
    private aLastDtsSec;
    private vMinPtsSec;
    private aMinPtsSec;
    private fragmentedVideoSeen;
    private fragmentedAudioSeen;
    private fragmentedAudioPacketCount;
    private fragmentedAudioDurationUnits;
    private videoTimelineRebaseSec;
    private audioTimelineRebaseSec;
    private videoPresentationStartSec;
    private audioPresentationStartSec;
    private readonly eagerStandard;
    private standardHeaderWritten;
    private standardMdatHeaderOffset;
    private standardPayloadOffset;
    private standardMediaBytes;
    constructor(cfg: MuxerConfig, sink: Sink);
    /** Appends one encoded video chunk; `codecConfig` on the first call carries the decoder configuration record. */
    addVideoChunk(chunk: EncodedChunk, codecCfg?: Uint8Array): void;
    private normalizeVideoChunk;
    addExtraVideoChunk(index: number, chunk: EncodedChunk, codecCfg?: Uint8Array): void;
    /** Appends a chunk to the extra (copied) audio track at `index`. */
    addExtraAudioChunk(index: number, chunk: EncodedChunk, codecCfg?: Uint8Array): void;
    /** Appends one encoded audio chunk; `codecConfig` on the first call carries decoder configuration where the container stores it. */
    addAudioChunk(chunk: EncodedChunk, codecCfg?: Uint8Array): void;
    private storeStandardSample;
    private ensureEagerStandardHeader;
    private finalized;
    private audioPriming;
    setAudioPriming(primeSamples: number, validSamples: number, presentationTimestamps?: boolean, _discardLeadingSamples?: boolean, codecDelaySamples?: number): void;
    /** Supplies the audio decoder configuration (e.g. AAC AudioSpecificConfig) before finalize(). */
    setAudioCodecConfig(codecConfig: Uint8Array): void;
    /** Flushes trailing container structures and closes the sink; must be awaited exactly once. */
    finalize(): Promise<void>;
    private assertVideoDecoderConfig;
    private writeEagerStandard;
    private writeStandard;
    private checkedStandardByteCount;
    private writeInitSegmentOnce;
    private fragmentPresentationStart;
    private validateFragmentedAudioPresentationWindow;
    private fragmentTimelineRebase;
    private isH264;
    private buildFragTrak;
    private buildTrex;
    private fragmentDecodeBase;
    private flushFragment;
    private buildMfhd;
    private buildFtyp;
    private buildMoov;
    private buildMvhd;
    private buildTrak;
    private buildEdts;
    private writeTkhdMatrix;
    private buildTkhd;
    private languageCode;
    private buildMdhd;
    private buildHdlr;
    private buildMinf;
    private buildDinf;
    private buildStbl;
    private buildStsd;
    private buildVideoSampleEntry;
    private buildAudioSampleEntry;
    private buildMp4aEntry;
    private buildDolbyAudioEntry;
    private resolveVideoSampleEntryType;
    private buildEsds;
    private buildStts;
    private buildCtts;
    private buildStsc;
    private buildStsz;
    private buildStco;
    private buildStss;
    private videoTimescale;
    private audioTimescale;
}
