export default {
    tabWidth: 4,
    singleQuote: true,
    semi: true,
    printWidth: 120,
    trailingComma: 'all',
    arrowParens: 'avoid',
    overrides: [{ files: ['*.json', '*.yml', '*.yaml'], options: { tabWidth: 2 } }],
};
